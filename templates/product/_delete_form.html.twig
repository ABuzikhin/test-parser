<form method="post" action="{{ path('app_product_delete', {'id': productId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ productId) }}">
    <button class="btn btn-danger">Delete</button>
</form>
